header.header
    .header__column
        a(href=routes.home)
            i.fab.fa-youtube
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholder="Serching keyword", name ="keyword")
    .header__column
        ul
            li 
                a(href=routes.join) join
            li 
                a(href=routes.login) login